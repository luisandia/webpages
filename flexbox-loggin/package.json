{
  "name": "login_inicio",
  "version": "1.0.0",
  "description": "login with flexbox, gulp and sass",
  "main": "gulpfile.js",
  "scripts": {
    "development:gulp": "gulp watch",
    "development:live-server": "live-server",
    "dev": "npm-run-all --parallel development:*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Juan De la torre",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-autoprefixer": "^6.1.0",
    "gulp-sass": "^4.0.2",
    "live-server": "^1.2.1",
    "npm-run-all": "^4.1.5"
  }
}
